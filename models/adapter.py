"""Trainable token adapter (Phase A.2).

2-layer MLP: 1024 -> 1024 -> 512 (GELU), per-token, view-shared.
Also provides noise_augment() for decoder path.
"""

raise NotImplementedError("A.2: adapter.py — to be implemented")
